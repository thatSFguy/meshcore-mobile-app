<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Compose draws everything; this just avoids the stock ActionBar
         flash before setContent runs. -->
    <style name="Theme.MeshCore" parent="android:Theme.Material.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">false</item>
    </style>
</resources>
